# claude-installer

This project provides a minimal wrapper and documentation for installing Claude via the official install script.

## Install

Run the following command:

```bash
curl -fsSL https://claude.ai/install.sh | bash
```
